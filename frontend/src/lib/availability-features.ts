// Owner-requested simplified calendar. Re-enable individually when requested.
// These are display switches, not API authorization controls.
export const availabilityFeatures = {
  demoPriceCycles: false,
  stayQuote: false,
  pricingReview: false,
};
